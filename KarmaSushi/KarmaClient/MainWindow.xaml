<Window x:Class="KarmaClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KarmaClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <TabControl x:Name="TabColumn" Grid.Column="0">
            <TabItem Header="Menu">
                <ScrollViewer>
                    <WrapPanel x:Name="MenuPanel" Orientation="Horizontal">                        
                    </WrapPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Placeholder">
                <Grid>
                    <Button FontSize="8" Margin="23,10,0,0"/>
                </Grid>
            </TabItem>
        </TabControl>

        <Grid Grid.Column="1">
            <ListBox x:Name="OrderList" HorizontalAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3"/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <Label Grid.Column="0" Content="{Binding Name}"/>

                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
